import 'package:flutter/material.dart';

class WowSoEmpty extends StatelessWidget {
  const WowSoEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Wow so empty🌻", style: TextStyle(fontSize: 20)));
  }
}
