enum Pages { profile, settings, chat, chats }

final pagesWrapper = {
  Pages.profile: 'Profile',
  Pages.settings: 'Settings',
  Pages.chat: 'Chat',
  Pages.chats: 'Messages',
};

enum Status { online, offline }

final statusWrapper = {Status.online: 'Online', Status.offline: 'Offline'};
